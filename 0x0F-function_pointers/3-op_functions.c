#include "3-calc.h"
/**
 * op_add - does a thing
 * @a: first thing
 * @b: second thing
 * Return: what happens with the thing
 */
int op_add(int a, int b)
{
        return (a + b);
}
/**
 * op_sub - does a thing
 * @a: first thing
 * @b: second thing
 * Return: what happens with the thing
 */
int op_sub(int a, int b)
{
        return (a - b);
}
/**
 * op_mul - does a thing
 * @a: first thing
 * @b: second thing
 * Return: what happens with the thing
 */
int op_mul(int a, int b)
{
        return (a * b);
}
/**
 * op_div - does a thing
 * @a: first thing
 * @b: second thing
 * Return: what happens with the thing
 */
int op_div(int a, int b)
{
        return (a / b);
}
/**
 * op_mod - does a thing
 * @a: first thing
 * @b: second thing
 * Return: what happens with the thing
 */
int op_mod(int a, int b)
{
        return (a % b);
}
