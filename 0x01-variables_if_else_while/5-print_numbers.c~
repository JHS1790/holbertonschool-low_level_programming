#include <stdio.h>

/**
 * main - declares n as positive or negative or 0
 *
 *
 * Return: Ends the program
 */

int main(void)
{

	char ch;

	for (ch = '1' ; ch < '10' ; ch++)
	{
		putchar(ch);
	}

	putchar('\n');

	return (0);
}
